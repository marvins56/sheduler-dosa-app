@model IEnumerable<sheduler.Models.semester>

@{
    ViewBag.Title = "Manage Semesters";
    Layout = "~/Views/Shared/adminlayout.cshtml";
}

@{
    if (Session["userid"] != null)
    {
        <div class="container-xxl flex-grow-1 container-p-y">
            <!-- Hoverable Table rows -->
            <div class="card">
                <h5 class="card-header">Manage Semesters</h5>
                <div class="table-responsive text-nowrap">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Semester_taken)
                                </th>
                                <th> <a href="@Url.Action("Create","semesters")" class="btn btn-primary">  Add semester</a></th>

                                <th></th>
                            </tr>
                        </thead>
                        <tbody class="table-border-bottom-0">
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Semester_taken)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Edit", "Edit", new { id = item.sem_id }) |
                                        @Html.ActionLink("Delete", "Delete", new { id = item.sem_id })
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

    }
    else
    {
        <script>
            swal({
                title: "NOT AUTHORISED",
                text: "ACCESSDE DENIED",
                icon: "error"
            });</script>

    }
}

